# Calculating Euclidean and Manhattan distance in python
import numpy as np
# consider two points with 2D
p1 = (1,1)
p2 = (2,2)
# calculating euclidean distance
dist = ( p1[0] - p2[0])**2 + ( p1[1] - p2[1])**2
euclidean_dist = np.sqrt(dist)
print("Euclidean Distance: ", euclidean_dist)


import numpy as np
# consider two points with 3D
p1 = (1,1,1)
p2 = (2,2,2)
# Caclulating euclidean diatance
dist1 = ( p1[0] - p2[0])**2 + ( p1[1] - p2[1])**2 + ( p1[2] - p2[2])**2
euclidean_dist1 = np.sqrt(dist1)
print("Euclidean Distance: ", euclidean_dist1)

import numpy as np
# consider two points with 4D
p1 = (1,1,1,1)
p2 = (2,2,2,2)
# Caclulating euclidean diatance
dist4 = 0
for i in range( len(p1)):
    dist4 += ( p1[i] - p2[i])**2
euclidean_dist4 = np.sqrt(dist4)
print("Euclidean Distance: ", euclidean_dist4)


# creating a function for calcultaing euclidean distance
def eucl_dist(p1, p2):
    dist =0
    for i in range( len(p1)):
        dist += ( p1[i] - p2[i])**2
    eucl_dist = np.sqrt(dist)
    print( eucl_dist   ) 
eucl_dist( (1,2,3), (4,5,6) )




# calculating manhattan distance
import numpy as np
# creating a function for calculating manhattan distance
def manh_dist(p1, p2):
    dist =0
    for i in range( len(p1)):
        dist += abs( p1[i] - p2[i])
        manh_dist = dist
    print( manh_dist) 
manh_dist( (1,2,3), (4,5,6) )
# abs() function can also be used to calculate absolute value of given integers/number
